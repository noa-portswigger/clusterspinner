apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-dns
  namespace: external-dns
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::{{account_id}}:role/{{cluster_name}}-external-dns"
automountServiceAccountToken: true
